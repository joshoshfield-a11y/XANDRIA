# Node and build artifacts
node_modules/
dist/
dist-electron/
dist-*
.vscode/
.env
.env.local
.env.*.local
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # optionally ignore if you choose not to commit lockfile (but for apps it's typical to commit)
# Capacitor/Android build artifacts
android/
ios/
# Keystore and other secrets
*.keystore
debug.keystore
# OS files
Thumbs.db